using System;

//Extra creativity: Added 5 levels based on score. Required a Levels class, determine level method, list, and console.writeline witihin the GoalManager class.
class Program
{
    static void Main(string[] args)
    {
        GoalManager newGoalManager = new GoalManager();

        newGoalManager.Start();
    }
}